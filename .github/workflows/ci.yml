name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  electron-pack:
    name: Electron 打包
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Electron app
        run: pnpm electron:build
        env:
          # CI 无签名证书，跳过 macOS 代码签名
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Check build output
        run: |
          release_dir="apps/editor/release"
          if [ ! -d "$release_dir" ]; then
            echo "Error: release directory not found"
            exit 1
          fi
          echo "Release files:"
          ls -la "$release_dir/"
          # universal 构建输出 release/mac/，单架构为 mac-arm64/ 等
          if ! find "$release_dir" -maxdepth 2 -type d -name "MD Render.app" | grep -q .; then
            echo "Error: MD Render.app not found"
            find "$release_dir" -maxdepth 2 -type d | head -20
            exit 1
          fi
          if ! ls "$release_dir"/*.dmg >/dev/null 2>&1; then
            echo "Error: dmg file not found"
            exit 1
          fi

      - name: Upload Electron artifact
        uses: actions/upload-artifact@v4
        with:
          name: electron-mac
          path: |
            apps/editor/release/*.dmg
            apps/editor/release/*.blockmap
          retention-days: 7
