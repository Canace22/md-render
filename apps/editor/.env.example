# ── Notion 同步代理地址 ──────────────────────────────────────
#
# 指向你服务器上的 Notion 转发服务（部署见 server/notion-proxy/README.md）。
# 末尾必须是 /v1，不要带斜杠结尾。生产建议用 https。
#
# VITE_NOTION_PROXY=https://your-server.example.com/notion-proxy/v1

# ── AI 代理服务地址 ─────────────────────────────────────────
#
# Electron 主进程通过此地址调用 ai-proxy server 的 /api/chat。
# API Key 存在 server 的 .env 里，不在此文件中。
#
# 开发默认 http://localhost:8788，生产改为你的服务器地址。
# AI_PROXY_BASE=http://localhost:8788
